<% content_for :title, "New group" %>

<div class="mx-auto max-w-2xl rounded-2xl border border-gray-200 bg-white p-6 shadow-sm">
  <h1 class="text-2xl font-semibold text-gray-900">새 그룹 만들기</h1>
  <div class="mt-6">
    <%= render "form", group: @group %>
  </div>
</div>
