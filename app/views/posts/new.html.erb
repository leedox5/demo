<% content_for :title, "새 항목" %>

<div class="md:w-2/3 w-full">
  <h1 class="nd-heading text-4xl font-semibold text-[color:var(--nd-ink)]">새 항목 추가</h1>

  <%= render "form", post: @post %>

  <%= link_to "항목 목록으로", posts_path, class: "mt-2 inline-block w-full rounded-md border border-[color:var(--nd-line)] bg-[color:var(--nd-surface)] px-3.5 py-2.5 text-center font-medium text-[color:var(--nd-ink)] hover:bg-[color:var(--nd-sidebar)] sm:ml-2 sm:mt-0 sm:w-auto" %>
</div>
